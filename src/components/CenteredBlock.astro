---
interface Props {
  maxWidth?: string;
  class?: string;
}

const {
  maxWidth = "max-w-3xl",
  class: className = "",
} = Astro.props;
---

<div class:list={["mx-auto text-center", maxWidth, className]}>
  <slot />
</div>
